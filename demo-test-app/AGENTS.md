# Demo Test App AGENTS

Last updated: 2026-04-25

Scope: `demo-test-app/`

## Core Rules
- Keep demo workflows aligned with real backend behavior.
- Use this app for validation and demos, not as a source-of-truth architecture layer.

## Commands
- Dev server: `npm run dev`
- Build: `npm run build`
- Checks: `npm run check && npm run lint`

## Required Verification
- Run `npm run check` and `npm run lint` for touched app logic/components.
